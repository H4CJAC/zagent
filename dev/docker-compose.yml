# Development Environment for CclawCore Agentic Testing
#
# Use this for:
# - Running the agent in a sandboxed environment
# - Testing dangerous commands safely
# - Developing new skills/integrations
#
# Usage:
#   cd dev && ./cli.sh up
#   or from root: ./dev/cli.sh up
name: cclawcore-dev
services:
  # ── The Agent (Development Image) ──
  # Builds from source using the 'dev' stage of the root Dockerfile
  cclawcore-dev:
    build:
      context: ..
      dockerfile: Dockerfile
      target: dev
    container_name: cclawcore-dev
    restart: unless-stopped
    environment:
      - CCLAWCORE_GATEWAY_PORT=42617
      - SANDBOX_HOST=cclawcore-sandbox
    secrets:
      - source: cclawcore_env
        target: cclawcore_env
    entrypoint: ["/bin/bash", "-lc"]
    command:
      - |
        if [ -f /run/secrets/cclawcore_env ]; then
          set -a
          . /run/secrets/cclawcore_env
          set +a
        fi
        exec cclawcore gateway start --port "${CCLAWCORE_GATEWAY_PORT:-42617}" --host "[::]"
    volumes:
      # Mount single config file (avoids shadowing other files in .cclawcore)
      - ../target/.cclawcore/config.toml:/cclawcore-data/.cclawcore/config.toml
      # Mount shared workspace
      - ../playground:/cclawcore-data/workspace
    ports:
      - "127.0.0.1:42617:42617"
    networks:
      - dev-net

  # ── The Sandbox (Ubuntu Environment) ──
  # A fully loaded Ubuntu environment for the agent to play in.
  sandbox:
    build:
      context: sandbox # Context relative to dev/
      dockerfile: Dockerfile
    container_name: cclawcore-sandbox
    hostname: dev-box
    command: ["tail", "-f", "/dev/null"]
    working_dir: /home/developer/workspace
    user: developer
    environment:
      - TERM=xterm-256color
      - SHELL=/bin/bash
    volumes:
      - ../playground:/home/developer/workspace # Mount local playground
    networks:
      - dev-net

networks:
  dev-net:
    driver: bridge

secrets:
  cclawcore_env:
    file: ../.env
