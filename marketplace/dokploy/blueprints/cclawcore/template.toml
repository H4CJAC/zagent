[variables]
main_domain = "${domain}"
api_key = "${password:64}"

[config]
env = [
  "API_KEY=${api_key}",
  "PROVIDER=openrouter",
  "CCLAWCORE_ALLOW_PUBLIC_BIND=true",
  "CCLAWCORE_GATEWAY_PORT=42617"
]

[[config.domains]]
serviceName = "cclawcore"
port = 42617
host = "${main_domain}"
